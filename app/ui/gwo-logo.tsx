import { lusitana } from '@/app/ui/fonts';

export function GwoLogo() {
  return (
    <div className={`${lusitana.className} flex flex-row`}>
      <p className="text-xl font-bold text-gray-500 md:text-3xl">G.W.O.</p>
    </div>
  );
}
